version: v1beta1
name: buf.build/recap/arg-services
deps:
  - buf.build/googleapis/googleapis
build:
  roots:
    - proto
lint:
  ignore_only:
    ENUM_ZERO_VALUE_SUFFIX:
      - arg_services/graph/v1/graph.proto
  use:
    - DEFAULT
